# we'll import the http module for web functionality
(import "http.arkm")

# the port we will listen to
(let port 3000)

# let's create a server
(let srv (http:server:create))

# here we set up a route handler at root "/"
# that will return the simple html content
# with Content-Type: "text/html" 
(http:server:get srv "/" "<h1>I love Ark on web!</h1>" "text/html")

# here we listen at port 3000 on localhost
(print "listening on port " port)
(http:server:listen srv "localhost" port)
